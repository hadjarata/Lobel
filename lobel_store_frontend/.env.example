# URL du backend Django (production uniquement).
# En développement, laisser vide : Vite proxifie /api et /media automatiquement.
# VITE_API_BASE_URL=

# Cible du proxy Vite en dev (sur le PC, pas l'IP du téléphone).
# VITE_DEV_BACKEND_TARGET=http://127.0.0.1:8000
